{
  "name": "ai-tools-directory-root",
  "private": true,
  "scripts": {
    "start": "npm start --prefix backend",
    "build": "npm install --prefix backend"
  },
  "engines": {
    "node": "20.x"
  }
}
