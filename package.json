{
  "name": "ai-tools-directory",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build -w frontend && (mkdir dist || true) && cp -r frontend/dist/* dist/",
    "start": "npm start -w backend"
  },
  "engines": {
    "node": ">=20.x"
  }
}
